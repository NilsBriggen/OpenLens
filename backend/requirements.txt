# OpenLens backend requirements (FastAPI gateway + service layer).
# Floors, not pins: the old numpy/pandas pins had no wheels for modern Python.

# API gateway
fastapi>=0.110
uvicorn>=0.27
websockets>=12.0
python-multipart>=0.0.7
pydantic>=2.6
python-dotenv>=1.0

# Auth
PyJWT>=2.8
bcrypt>=4.1
passlib>=1.7.4

# Data / graph / ML (CPU)
neo4j>=5.28,<6
networkx>=3.4
numpy>=2.3
pandas>=2.3
scikit-learn>=1.7
rapidfuzz>=3.9

# Scraping / HTTP
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0

# Async processing
celery>=5.3
redis>=5.0

# Crypto & system
cryptography>=42.0
psutil>=5.9

# Testing
pytest>=8.0
pytest-cov>=4.1
httpx>=0.27
